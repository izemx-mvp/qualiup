import {createFileRoute} from '@tanstack/react-router';import {z} from 'zod';import {ContactPage} from '@/components/pages/ContactPage';export const Route=createFileRoute('/contact')({validateSearch:z.object({secteur:z.string().optional(),type:z.string().optional()}),head:()=>({meta:[{title:'Contact et demande de devis — QualiUp Group'},{name:'description',content:'Contactez QualiUp pour une analyse, un devis, une formation ou un audit au Maroc.'}]}),component:ContactPage});
